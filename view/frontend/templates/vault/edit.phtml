<?php
/** @var \Swarming\SubscribePro\Block\Vault\Edit $block */
/** @var \Magento\Framework\Escaper $escaper */
?>
<form class="form-address-edit form-vault-edit"
      action="<?= $escaper->escapeUrl($block->getSaveUrl()) ?>"
      method="post"
      id="vault-edit"
      enctype="multipart/form-data"
      data-hasrequired="<?= $escaper->escapeHtml(__('* Required Fields')) ?>">

    <?= /** @noEscape */ $block->renderBillingAddress() ?>

    <?= /** @noEscape */ $block->renderCard() ?>

    <div class="actions-toolbar">
        <div class="primary">
            <button type="submit" class="action save primary" data-action="save-vault" title="<?= $escaper->escapeHtml(__('Save Card')) ?>">
                <span><?= $escaper->escapeHtml(__('Save Card')) ?></span>
            </button>
        </div>
        <div class="secondary">
            <a class="action back" href="<?= $escaper->escapeUrl($block->getBackUrl()) ?>"><span><?= $escaper->escapeHtml(__('Go back')) ?></span></a>
        </div>
    </div>
</form>
<script type="text/x-magento-init">
    {
        "#vault-edit": {
            "validation": {}
        }
    }
</script>
